import { HardhatRuntimeEnvironment } from 'hardhat/types'
import { DeployFunction } from 'hardhat-deploy/types'

const deployCitrusToken: DeployFunction = async function deployCitrusToken({}: HardhatRuntimeEnvironment) {}

export default deployCitrusToken
deployCitrusToken.id = '001_citrus_token'
deployCitrusToken.tags = []
